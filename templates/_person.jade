mixin person(name, twitter, about, comment)
  img(src="images/#{name.toLowerCase()}.png")
  .name=name
  .twitter
    a(href="http://twitter.com/#{twitter}") @#{twitter}
  p.about=about
  p
    small (#{comment})
